# Gemfile for media-organizer
source 'https://rubygems.org'

# taglib-ruby has a dependency on the taglib core library. Install taglib using, for example:
# dnf install taglib
# brew install taglib
# apt-get install taglib
gem 'taglib-ruby', '~>0.7.1'

# exifr: file MIME type detector
gem 'exifr', '~>1.2.4'
gem 'rake', '~>11.1.2'
gem 'rubocop',  '~>0.39.0'
gem 'minitest', '~>5.8.4'
